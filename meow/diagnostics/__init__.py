"""Asking the app about itself: keys, connectors, and the ablation.

A diagnostic answers a question a log cannot: not "what did it do" but "what
can it currently reach". Kept apart from `meow.testing`, which asks whether
the code is correct - these ask whether the environment is.
"""
