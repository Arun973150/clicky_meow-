"""Finding things out, and writing them down.

`research` is the component that reads untrusted pages, and it holds search
and fetch and NOTHING else - no files, no desktop, no outbound. `recipes`
is the other half of that bargain: a capability can be a markdown file
rather than a release.
"""
